export interface BuyerInfo {
  name: string;
  email: string;
  cpf: string;
}

export interface AddressInfo {
  cep: string;
  street: string;
  neighborhood: string;
  city: string;
  state: string,
}
