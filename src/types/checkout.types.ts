export interface BuyerInfo {
  name: string;
  email: string;
  cpf: string;
}

export interface AddressInfo {
  cep: string;
  logradouro: string;
  bairro: string;
  cidade: string;
  uf: string,
  numero: string
}
